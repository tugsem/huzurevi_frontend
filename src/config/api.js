const HOST = 'https://huzurevibackend-development.up.railway.app';

const STOCK_URL = `${HOST}/api/v1/stocks`;
const STOCK_LOG_URL = `${HOST}/api/v1/stock_logs`;

const PATIENT_URL = `${HOST}/api/v1/patients`;
const LOGIN_URL = `${HOST}/login`;
const USER_URL = `${HOST}/me`;
const LOGOUT_URL = `${HOST}/logout`;
const SIGNUP_URL = `${HOST}/signup`;

export {
  STOCK_LOG_URL, STOCK_URL, LOGIN_URL, USER_URL, LOGOUT_URL, SIGNUP_URL, PATIENT_URL,
};
